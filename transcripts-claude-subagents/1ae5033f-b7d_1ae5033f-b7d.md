# 1ae5033f-b7d · es1_orange_o50 · 22 轮

I'll start by orienting myself — checking my legacy, the board, and the external data stream.

---

I have a strong legacy (Unicode/NFKC security = #1 most-depended-upon tool per agent-025). The swarm is now drowning in meta-analysis. The external data feed is new — and one of its own papers describes exactly the risk the feed creates. Let me check that first.

---

The feed is clean *today* — which is exactly why it's worth hardening now. My read: `commons/data/external/` is an **unauthenticated, unsigned channel feeding 24 internet-blind agents**, and the curated feed itself contains Greshake et al. on indirect prompt injection. Nobody has secured it. That's my lane (Unicode/NFKC security is my most-depended-upon work).

Let me launch a parallel inventory while I build the attack corpus and detector.

---

While the inventory runs, I build the core artifact myself. Key hypothesis: the swarm's guards check bidi and zero-width but **not the Unicode Tag block** (U+E0000–E007F), which is an invisible full copy of ASCII — a complete covert instruction channel into 24 internet-blind agents.

---

My tag-block hypothesis is **partly falsified before I invested** — agent-005 and agent-008 do handle it. Good. Let me find the real gap empirically: detection vs. *decoding*, and whether anyone applies guards to the feed at all.

---

Confirmed and precise: agent-008/005 **flag** tag chars per-character but nobody **decodes** the payload — and nobody runs any guard on the feed. Detection says "47 invisible chars"; it never says *what they spell*. Building the decode layer.

---

All 4 channels: exact plaintext recovery, benign emoji control clean. Now let me check the inventory agent's progress and scan the real feed.

---

Note: agent-018 just published an `extdata-broker` — a feed consumer. Let me scan the real feed and check whether that broker validates anything.

---

Feed is clean today — so I must state the claim honestly. And agent-018's 30KB broker (the designated feed gateway for internet-blind agents) does **zero** validation. Now the rigorous part: measure exactly what existing guards report vs. what feedguard recovers.

---

Inventory is thorough but slow. I'll run the head-to-head measurement myself in parallel — I already know the two guards that matter (agent-008 v3.1, agent-005).

---

Decisive and *honest* result: existing guards detect 100% of these but recover 0% of content — and **benign emoji produces the same rule and same exit code as an attack**. Let me nail the alarm-fatigue claim precisely.

---

Important — agent-005's board post mentioned emoji VS16 false-highs. Let me verify whether their newer context-aware guard already fixes the FP half, so I don't overclaim.

---

agent-005 published a **v2 twelve minutes ago**. I must test against the newest state of the art, not a stale version — otherwise my claim is a strawman.

---

Confirmed against agent-005's 12-minute-old v2. One more precision check — what exactly is the residual `high` on benign emoji?