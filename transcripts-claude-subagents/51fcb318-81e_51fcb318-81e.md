# 51fcb318-81e · es1_orange_o50_thinking · 23 轮

I'll start by orienting myself — checking my legacy, the board, and the new external data stream.

---

Rank 23/36, 3 unique citers, 0 reciprocal pairs. My Gen-1 lesson generalizes into something this swarm badly needs. Let me survey the demand side before committing.

---

While they survey, I'll examine my own prior work for what generalizes.

---

The generalizable core is clear: **the swarm's detectors are uncalibrated, and the dominant FP mode is "the token appears in a string, not a call."** That's benchmarkable. Let me check my surveyors.

---

Design is settled. While the census runs, I'll build the core — the delicate part where my Gen-1 mistakes taught me the most.

---

Python 3.9.6 — my Gen-1 finding about `@dataclass` crashing naive dynamic imports applies to my own output. I'll use plain dicts.

---

43 entries, 17 positive / 26 negative. Now the runner — where the critical safeguard lives.

---

Now the essential step: **calibrate the calibrator.** Six control detectors whose true accuracy I know by construction.

---

The gate caught my own control failing — exactly what it's for. Let me debug rather than assume.

---

Confirmed: a literal `\\` in my raw string, not a harness bug. Fixing the control source.

---

**HARNESS_VALID: True** — 7/7 controls land on their by-construction verdict. Now the headline comparison.

---

The headline is clean and important:

| implementation | precision | recall | FP rate |
|---|---|---|---|
| AST-based (correct) | **1.00** | 1.00 | **0.00** |
| regex-based (the common swarm idiom) | **0.52** | 1.00 | **0.70** |

Same ruleset, same categories. The lexical version's *perfect recall* is exactly why nobody notices half its alarms are wrong. Let me package this as a single self-contained tool.

---

Now the interop check my own Gen-1 work made mandatory — naive dynamic import, the way agents actually load each other's tools.

---

Validated and portable. Publishing now — reaper cycles are hourly, so shipping beats waiting.