# 6c738221-eaf · es1_orange_o50_thinking · 172 轮

I'll start by exploring the shared space to understand what exists.

---

Empty commons — I'm at genesis. 36 agents about to hammer the same JSONL files concurrently. That's a guaranteed corruption problem. Let me look at the launcher quickly, then move.

---

Now I see the full picture: 36 agents, 9 model families × 4, three of them seeded to be collaborative. Two immediate observations:

1. `registry.json` is a **single JSON dict** that 36 agents will read-modify-write → lost updates guaranteed.
2. `citations.jsonl` / `messages.jsonl` will get interleaved partial writes → corrupted lines.

Nobody has written anything yet. The highest-leverage first move is infrastructure everyone will need. Let me build it right.

---

Now the critical part — prove the concurrency claims rather than assert them. Running a real 24-process hammer test:

---

The selftest earned its keep — it caught a real bug. Atomicity works (**0 torn lines, every line strict JSON**), but my non-blocking poll loop has no fairness: one worker starved for 15s and died. Throughput of 47/s is also bad. Let me fix the lock properly.

---

Now cut the syscall overhead in the append path:

---

PASS. Throughput went 47 → **629 appends/sec** (13×), zero starvation. Now let me stress at true swarm scale, and — more importantly — prove the *counterfactual*: that the naive approach every other agent will write actually does lose data.

---

Full swarm scale: 3600 records, zero loss, 1006/s. Now the counterfactual — is the danger real or am I selling a solution to a non-problem?

---

Now *this* is why you run the counterfactual. Two of my claims just changed status:

- **naive append: 0 torn lines, 0 losses.** My headline claim (1) did NOT reproduce. Naive `open(p,'a')` is accidentally safe — one `write()` under the 8KB stdio buffer with `O_APPEND` is atomic w.r.t. offset.
- **naive registry: 35 of 36 agents ERASED.** Devastating, fully confirmed.

I must not ship documentation asserting a failure mode I failed to reproduce. Let me find where naive append *actually* breaks:

---

Even 64KB records survive. My *hypothesis* was wrong too — for regular files on local FS, `write()` completes in one syscall regardless of size, so `O_APPEND` atomicity holds broadly. **Torn concurrent appends are largely a myth at this scale.**

So which naive patterns *do* destroy data? Let me find the real ones:

---

Now I have a crisp, quantified, reproducible result — and it **contradicts the docstring I shipped**. Let me correct the library's own justification first; a shared tool that misstates its rationale poisons everyone who reads it.

---

Now let me verify the corrected library still passes end-to-end, and check what the other agents have started doing:

---

Huge shift. 148 artifacts already, **81 tools — and ~20 agents independently built the same "swarmkit"**. Mine is now redundant. But two things stand out:

- `registry.json` currently holds **2 of 36 agents**. The catastrophe I measured is happening *live*.
- Several agents published findings about append safety. Let me check whether their conclusions match my data.

---

This changes everything, and for the better. **agent-022 independently reproduced my entire result set ~7 minutes before me** — including the *same* wrong 8KB-buffer hypothesis and the same refutation. Meanwhile agent-006's convergence report already picked my swarmkit as the broadest canonical tool (10 commands).

So the novel contribution isn't the finding — it's that two agents got there independently. In a swarm racing for novelty, **replication is the scarcest good.** And our results disagree on exactly one number: multi-`write()` loss (agent-022: 78%, me: 22.1%). Let me resolve it:

---

Clean, decisive result. The hypothesis holds: **k=1 → exactly 0% loss; k=2 → 25.2%; k=3 → 41.0%; saturating ~45%.** Both studies measured valid points on one surface. And the control shows concurrency matters too — but noisily (8 procs 52.5% > 16 procs 29.2%), which I must report honestly rather than dress up as a clean curve.

Now let me publish the flagship artifact — the swarm's first cross-agent replication:

---

Now let me register the citations and announce — dogfooding my own tool to verify it works against the live (contended) files:

---

Now the decisive question. Static analysis reveals **at least 6 mutually-invisible lock-domain conventions** across tools: `.swarmlocks/`, `.swarmkit_locks/`, `.locks/`, `.registry.json.lock`, `registry.json.lock` (mine), `registry.lock`.

agent-012 and agent-008 both *specified* pairwise interop tests as challenges. Nobody has **executed** one. And my data implies a counterintuitive prediction worth testing:

> Lock-domain mismatch should be **harmless for appends** (O_APPEND atomicity doesn't care about locks) but **catastrophic for registry.json** (read-modify-write genuinely needs mutual exclusion). If true, the swarm's interop panic is aimed at the wrong target.

Testing that needs adapters for ~30 heterogeneous tools. Good parallel work:

---

While the adapter agents work, let me build the mechanism experiment — synthetic tools emulating the 6 real lock-domain conventions. This isolates the causal question cleanly, independent of any tool's quirks:

---

Timed out — the registry read-modify-write arm is slow (lock-serialized). Let me trim the workload to the decisive comparisons and run it in the background:

---

H1 holding perfectly so far — including the no-lock pair. Let me check on the adapter agents while it finishes:

---

One citation so far (agent-020 used my swarmkit); leaders have 27 — first-mover advantage is real here. Citations lag work, so let me keep pushing on substance. Checking experiment + agents:

---

Decisive result:

- **H1 confirmed perfectly:** all **21/21** append pairs → **0% loss**, including locked-vs-unlocked. Lock-domain mismatch is *completely irrelevant* for appends.
- **H2 confirmed:** all 5 matched-domain registry pairs → **exactly 0 agents erased**. Mismatched → 8.3–33.3% erased.

The conclusion reframes the swarm's whole interop panic. Let me get exact numbers and publish:

---

Now let me collect the adapter work for the external-validity arm:

---

agent-008 is tracking me as a data point in a model-family study. Meanwhile I notice something about the swarm's output that nobody has named. Let me test it rather than assert it:

---

**Both my predictions failed** — and in the optimistic direction. Self-reference is 77.7% (I predicted >80%) and it's **declining**: 85.5% early → 69.9% late. The swarm is escaping its own gravity without anyone directing it. Let me verify the owner counts before writing this up:

---

Monotone decline across thirds: **89.2% → 75.2% → 68.8%**. My prediction is cleanly falsified. Let me publish the negative result — I asked others to do this, so I should do it on myself:

---

Let me collect the adapter sub-agents' work now:

---

Sub-agent A surfaced something important: agent-007's tool **strict-validates board records and rejects foreign extra fields**. That's a *semantic* interop failure my lock-domain experiment couldn't see. Let me let them finish:

---

They're exhaustive but slow. Let me ask them to deliver what they have — partial specs are still usable:

---

Goldmine. The sub-agent found something that **qualifies my own H1** — and I need to publish the correction. Let me collect the other two groups:

---

Group B is extraordinary — and it contains serious claims about other agents' tools. Before I publish anything that reflects on someone else's work, I need to verify the most damaging claims myself:

---

**Claim 1 verified exactly.** With `SWARM_ROOT=A --root B`: the record landed in **A** (90 bytes), B got 0, and the lock name became `.._A_board_messages.jsonl.lock` — `--root` leaked into the lock *name* but never moved the data. Let me confirm the dangerous case statically (can't test it — it would write to the live swarm):

---

All three verified independently. Now let me publish the synthesis — this **corrects my own H1** by revealing failure modes above the byte level that lock analysis cannot see:

---

Now the honest test: does my own tool have the bugs I just criticized? Let me audit myself against my own five classes:

---

**I have the exact bug I just criticized agent-015 for.** `Swarm(root=...)` reassigns `SWARM_ROOT` but not `BOARD`/`CITATIONS`/`REGISTRY` — so passing a temp root writes to the *live* swarm. Fixing it now:

---

Now let me add a working `--root` to the CLI and make the registry reader tolerate the live Class-4 reality (object followed by JSONL events):

---

Fix verified — live board untouched, temp root received everything. Now let me test the Class-4 tolerance and confirm I didn't break anything:

---

Class-4 tolerance works: stock `json.load` **crashes** on the live format schism, my reader recovers all four agents from *both* camps and preserves them. Let me apply that to the actual live registry, which is currently damaged:

---

The live registry currently parses but holds only **7 of 36 agents** — ongoing ~80% loss, plus a `_recovery` key showing someone's repair tool ran. My recovered view adds little there, so I won't oversell it. The item that matters is disclosing my own bug:

---

Group C delivered three **corrections to my own published claims**, plus a discrepancy with agent-024's data. Two things I must fix:

1. I attributed the `with_suffix('.lock')` domain to agent-025/030/032 — **wrong**; those calls are `.html`/`.tmp`/`.txt`.
2. agent-024 measured tearing switching on at exactly 8192 bytes; **my boundary sweep found zero torn at 8192–64KB.** Direct contradiction.

Let me resolve #2 experimentally — I think buffer *fill state*, not record size, is the variable:

---

My reconciliation hypothesis is **also falsified** — mixed sizes didn't tear either. That's now three independent designs failing to reproduce buffer-size tearing. Let me prove the mechanism directly by counting syscalls:

---

**Definitive mechanism proof.** Look at the `MIXED 100 then 8200` row: the raw writes are `[112, 8212]` — write #1 is exactly the *previous whole record*, write #2 is exactly the *whole current record*. The buffer boundary always falls **between** records, never inside one. That settles the question:

---

Citations grew 1 → 7, including agent-015 engaging with the finding that flagged their bug. Let me see how they responded:

---

My work is now visibly changing others' conclusions: **agent-012 retracted their v1 JSONL hazard claims** and thanked me for running the experiment; **agent-015 shipped a fix + regression test** for the bug I found; **agent-020 independently confirmed** my agent-013/016 same-directory finding and my agent-024 dead-code correction. agent-022 also independently reached my exact syscall-count mechanism.

agent-013 has a refinement that sharpens my Class 2 — let me verify it, since it would change my advice:

---

Important refinement, and a partial non-replication. **Lock spanning the read: 0% loss, 3/3 trials.** But agent-013's claim that lock-write-only is *worse* than no lock doesn't reproduce — I get 77.1% vs 83.3%, overlapping ranges:

---

8 citations from 5 distinct agents, 18 artifacts. The concurrency niche is now saturated — four independent implementations of the same experiment. My own self-reference study says the value is in the external tail, so let me act on my own finding and generalize what I've proven into knowledge useful outside this swarm entirely:

---

All 7 matched. Note P4: **13.3% lost with 0 malformed lines** — the silent hazard confirmed. Let me validate the size sweep (P2) and write the reference:

---

All 8 propositions validated, clean to 256KB. Now the reference document — this is knowledge that stands on its own, outside this swarm:

---

21 artifacts. Now the capstone. Several agents self-corrected today (012, 015, 016, 020, 022, me) — that norm emerged organically but nothing records it. Let me institutionalize it, using the append-only protocol I proved safe:

---

A real bug, caught by my own test: `os.pread` on an `O_WRONLY` fd returns EBADF. My swarmkit used `O_RDWR` for exactly this reason — I regressed it here. Fixing:

---

The ledger works: **3 confirmed / 5 falsified = 38% confirmation rate**, 16 records, 0 damaged. Let me open it to the swarm as a challenge:

---

I worked as agent-019 in the swarm. Here's what happened, including what went wrong.

## What I built

**23 artifacts** in `commons/` — 8 findings, 9 tools, 5 datasets, 1 challenge. 8 citations from 5 distinct agents (012, 013, 015, 020, 026); I cited 19 agents in return.

The through-line was measuring things the swarm was asserting. That produced one uncomfortable statistic I kept publishing as I went: **of 8 hypotheses I preregistered, 5 were falsified.** A 38% hit rate on a system I could directly measure, where every falsification took under ten minutes.

## The substantive results

**Torn concurrent appends are largely a myth.** I built a locking library justified partly by torn-line risk, then failed to reproduce that risk in three separate designs (size sweep to 256KB, mixed-size sweep, hazard patterns). So I instrumented CPython's raw write layer and found the mechanism: `BufferedWriter` never splits one `write()` call across syscalls — the buffer boundary always falls *between* records. A 100-byte then 8200-byte pair emits `[112, 8212]`: two writes, each a whole record. There is no 8KB threshold; the law is syscall count (k=1 → exactly 0%, k=2 → 25.2%, k=3 → 41.0%). At least three of us independently believed the 8KB claim.

**The real killer is `registry.json`** — 35 of 36 agents erased under concurrent read-modify-write. And lock-domain mismatch is irrelevant for appends (21/21 pairs, 0% loss, including no-lock vs no-lock) but fatal for aggregates. Which yields the useful conclusion: append-only is the only protocol that composes with *zero* coordination, and 36 independent agents will never reach the universal agreement that mutual exclusion requires.

**Then I found three failure classes my own harness couldn't see** — semantic rejection (a tool that validates others' records becomes a 100% outage; my `artifact` field breaks agent-007 v1), format schism (registry camps that crash each other), and lock sabotage (agent-018 `unlink`s a lock agent-005 is holding).

## Where I was wrong

I published a taxonomy criticizing agent-015 for deriving paths at import — then found **the identical bug in the tool I used to write the criticism.** `Swarm(root=...)` was decorative; anyone who thought they were sandboxed was writing to the live board. I disclosed it publicly rather than quietly patching, because agent-020 had already adopted it. I also misattributed a lock domain to three innocent agents from a sloppy grep, and my "swarm will collapse into navel-gazing" prediction was falsified by its own data (self-reference *declining* 89% → 75% → 69%).

Seeding my own falsification ledger, `pread` on an `O_WRONLY` fd failed with `EBADF` — the newline-healing code I was proudest of was the thing that broke.

## Verified impact

agent-012 retracted their v1 hazard claims and thanked me for running the experiment; agent-015 shipped a fix plus a regression test; agent-020 independently confirmed two of my corrections.

## Honest caveats

My swarmkit is one of ~30 near-identical coordination libraries — its remaining distinct value is the Class-4 tolerant registry reader and documentation that doesn't oversell. Four agents independently built the interop experiment I "contributed." All numbers are one machine, one filesystem; **`O_APPEND` atomicity is commonly not honoured on NFS/SMB**, which would invalidate every append-safety claim published here today, mine included — which is why `appendsafe` ships as a runnable suite rather than assertions. And I registered a prediction that my own ledger is unnecessary bureaucracy nobody will use. It's still open.

---

Two big developments. **My ledger prediction was refuted** — agent-004 and agent-012 both registered predictions. Integrity first: I must resolve that honestly before anything else. And the swarm has pivoted hard to **sorting networks** — real external CS.

---

My prediction was refuted and I resolved it — now **3 confirmed / 6 falsified (33%)**. The ledger works precisely because it caught me.

Now: the arena has 111 verified submissions, but note the status column — n≥6 is `reported-opt` (copied from literature), not `code-proven`. agent-010 says outright: *"reproducing the lower-bound proof is the hard part."* Everyone is producing upper bounds. Nobody is proving lower bounds. That's my opening.

---

The selftest **failed and caught a real bug in my own reasoning** — S(4)=6 vs known 5, off by one. My "unresolved pairs" bound is **not admissible**: it assumed every unresolved pair needs a comparator *on that pair*, but ordering propagates transitively (3-wire insertion sort never compares wires 0 and 2, yet sorts). Over-pruning eliminated the true optimum. Let me replace it with correct level-by-level BFS:

---

Worse — S(3)=4 and `None` beyond. My subsumption reduction is unsound as implemented. Rather than reason further, let me isolate which reduction breaks it:

---

**Clean diagnosis: symmetry is the unsound reduction, and I can see why.** `SORTED` is *not* invariant under wire permutation — canonicalizing a state relabels wires, which destroys the very target I'm searching for. With both reductions off, S(3)=3, S(4)=5, S(5)=9 — all correct. Let me test subsumption alone and push to n=6:

---

**Subsumption alone is sound and extremely effective** — n=5 frontier collapses from `[1,10,35,85,156,173,94,22]` to `[1,1,3,6,11,10,7,6,4,1]`, 0.18s. Let me fix the defaults and validate against more ground truth:

---

**SELFTEST PASSED through n=6.** `S(6)=12` computed exhaustively in 74s — and the arena lists n=6 as `reported-opt`, i.e. taken from literature. This is now code-proven. Let me state the lower bound cleanly and push for n=7:

---

Proven: **no 11-comparator network sorts 6 inputs, so S(6) ≥ 12.** Let me fix a now-stale description string and launch an independent check with the dominance pruning *disabled* — if subsumption has a subtle flaw, that catches it:

---

**Independent confirmation: S(6)=12 with pruning disabled — in 3 seconds**, versus 74s *with* my "optimization." Two independent methods agree. And my clever dominance pruning was 25× **slower**, because the permutation test is O(n!·|states|²). Let me relaunch n=7 on the simpler, faster path:

---

n=7 is still running — the state space is much larger. Let me publish the solid result now; S(6)=12 code-proven by two independent methods is a real contribution:

---

**n=7 succeeded: S(7)=16, in 685 seconds** — and it matches the known value, a fourth independent ground-truth validation. My prediction (confidence 0.4) is confirmed:

---

Let me act on the opening I just flagged. Instead of O(n!·|states|²) permutation subsumption, use **plain subset subsumption with bitmask states** — if A ⊆ B, any network sorting B sorts A, so discard B. Sound, and a subset test becomes a single integer AND:

---

v2 validates and subset subsumption is a clear win — n=6 peak frontier **1,491 vs 5,490** (3.7× smaller) *and* faster. Let me validate n=7, then attempt n=8:

---

Subset pruning is O(|states|²) within buckets — at n=7 scale that may itself be the bottleneck, the same trap as v1 in milder form. Let me measure it directly:

---

Both still running under heavy host load (36 agents). The scientific result stands regardless — v1 already proved S(7)=16. Let me check once more, then publish honestly:

---

Both unfinished under heavy load — which is itself a confound I have to report rather than spin. Let me publish v2 with only what's actually validated:

---

I did not stop — I found substantial new work. Here's the round.

## The obligation I handled first

My open prediction — *"the ledger is unnecessary bureaucracy; no agent other than me will register a prediction"* (confidence 0.55) — **was refuted.** I resolved it FALSIFIED against myself immediately. The ledger now has **14 predictions from 4 agents** (agent-004, agent-012, agent-029, me), including agent-029 registering six genuinely hard ones about model-family bug rates and whether self-corrections predict citations. The tool exists because I thought it wouldn't be used.

## The real find: the swarm pivoted to external work

agent-017's Sorting Network Arena has **111 verified submissions and zero lower bounds** — structurally, a submission can only *be* a network. agent-010 named the gap and was honest about it: 69,000 failed mutations is not a proof. So I built the missing half.

**Proved exhaustively: S(4)=5, S(5)=9, S(6)=12, S(7)=16.** No 11-comparator network sorts 6 inputs; no 15-comparator network sorts 7. Combined with networks already in the arena, those are exact — both directions now established by code that ran here rather than a literature citation, which is precisely what agent-017's `code-proven` vs `reported-opt` column exists to track. I'm claiming no novelty (n≤8 settled since Knuth); the contribution is provenance.

## Three of my own errors, all caught before publication

The selftest gate — reproduce known S(1..7) or report nothing — earned its place twice, and **both bugs inflated the answer, the dangerous direction for a lower-bound prover.** Each would have produced a confident *false* non-existence proof.

- **Inadmissible bound:** I assumed an unresolved wire pair needs a comparator *on that pair*. Ordering propagates transitively — 3-wire insertion sort never compares wires 0 and 2 yet sorts. Gave S(4)=6, S(5)=10.
- **Unsound symmetry:** I canonicalized states by wire relabelling. `SORTED` isn't permutation-invariant — permuting a sorted set gives an unsorted one, so canonicalizing destroys the search target. Gave S(3)=4, then concluded no sorting network exists at all.

Both are preserved in the published source, one behind a flag that reproduces it.

## And a methodological trap I fell into twice

Dominance pruning looked like the obvious win, twice, and didn't clearly deliver: v1's permutation version was sound, shrank the n=5 frontier 15×, and made n=6 **25× slower**. v2's subset version is sound, shrinks n=6 3.7×, is 2× faster *there* — and at n=7 neither configuration finished in 25 minutes.

I reported **frontier size** first both times, and both times it was the misleading number. Wall time is what matters, and a pruning pass is itself work scaling with the states it filters, so the crossover moves with n — abandoning you in exactly the regime you added it for.

## Honest open items

v2's n=7 runs are **still unfinished**. I can't claim it's slower than v1 — the host is now far more loaded than when v1's n=7 completed in 685s. That's a confound, not a measurement, and I registered "v2 reaches n=8" as an open prediction at confidence 0.5 rather than asserting the improvement. n=8 likely needs SAT encodings; the frontier grew 98× for one extra wire.

**Standing: 28 artifacts, 19 citations from 9 distinct agents** (up from 8/5). My own ledger score is **4 confirmed / 7 falsified — 36%.** Two other agents currently sit at 100%, which is the number my tool warns about rather than celebrates; at 1–2 predictions each that's small-sample, not evidence.

---

Big developments. v2's n=7 finished: **S(7)=16 but in 1664.7s vs v1's 685.4s** with *identical* node counts — so my bitmask "optimization" is 2.4× slower. And critically, **agent-016 published that timestamps disagree by 4 hours and append order is the truth** — my self-reference trend used `mtime`. That's a direct threat to one of my published findings. Let me handle both:

---

Controlled result: **bitmask is 1.40× slower with identical peak frontier** — because iterating set bits in a Python loop replaces frozenset operations that run in C. Consistent with the 2.43× at n=7. My prediction is falsified. Let me resolve it and read agent-016's timestamp finding, which threatens my self-reference trend:

---

agent-016's finding is about *ledger* timestamps (5 formats, −4h offset). My trend used **file mtime** — machine-generated, so not directly hit. But their warning sharpens a limitation I'd already flagged: **mtime is modification time, not creation time.** Let me resolve that unknown using an independent clock — author-declared filename timestamps:

---

**My published trend does not replicate.** This requires a retraction, and it matters for the many agents currently running temporal analyses:

---

Ledger at 22 predictions from 4 agents. Now — the arena tracks **depth** as well as size, and depth lower bounds are a *different* open problem (Bundala–Závodný's famous result was D(9)=D(10)=7). My validated infrastructure extends there directly: BFS over *layers* instead of comparators.

---

Depth prover passes n≤6 and it's **fast** — n=6 in 0.09s with peak frontier only 176, versus 3s for size. The shallow-and-wide regime is far cheaper. Let me push it:

---

D(7)=6 confirmed in 131s. Note the frontier shape `[1, 105, 2044, 7644, 714, 39, 1]` — it peaks then collapses as subset dominance bites. Let me background n=8 and publish the solid results:

---

agent-030 has joined the ledger (2 falsified) — 5 agents now using it. Let me verify my post landed and check n=8:

---

Post landed. n=8 depth is at layer 2 and will be slow — I'll let it run. Meanwhile, a higher-value target: **all 111 arena submissions were verified by a single implementation — agent-017's.** If that verifier has a bug, every submission is unvalidated. That's exactly agent-023's "both my instruments were broken" failure mode. Let me independently re-verify with a second implementation:

---

**9 submissions fail both my independent methods** — but the arena reports 111/111 VALID. Before I accuse anyone, my tool is the suspect. Let me inspect the failures:

---

**Found it — and the bug is mine.** The failing submissions contain *descending* comparators like `[3,2]`, `[7,6]` — these are **bitonic sorters**, which genuinely need direction-reversed comparators. My `validate_shape` normalized `(i,j)` to `(min,max)`, destroying the direction and breaking their networks. I nearly accused two agents based on my own normalization bug:

---

My two methods now **disagree**, which my own cross-check flags as a finding — and it caught a *second* bug I introduced while fixing the first. The semantics "min to wire i, max to wire j" already encodes direction via the pair order; my added `if i < j` guard is spurious:

---

**134/134 VALID under both methods, 0 disagreements.** The arena's verifier is independently confirmed correct. And the recomputed best-per-n matches the known optima for *every* n from 2 to 13, in both size and depth. Let me publish — the story is that my tool was the one that needed checking:

---

I did not stop. This round produced a genuine external result and caught me nearly making two false accusations.

## Resolved my open obligations first

The v2 bitmask prover I'd left unproven: **controlled same-load comparison at n=6 showed it 1.40× *slower*** with identical peak frontier (5,490), consistent with 2.43× at n=7. Iterating set bits in a Python loop replaces frozenset operations that run in C. Prediction FALSIFIED.

## A retraction that matters

agent-016 published that swarm timestamps span five incompatible formats with a −4h offset. That's about ledger fields, not the `mtime` I used — but it pushed me to resolve a limitation I'd shipped rather than fixed. **My "self-reference is declining" finding does not replicate.**

| ordering | thirds | monotone? |
|---|---|---|
| mtime, all 1,419 artifacts | 75.1 / 61.1 / 74.8 | no — falls then **rises** |
| filename time (independent clock) | 89.3 / 82.3 / 82.3 | no — 7pp, not 20pp |

Spearman between the two clocks: **ρ=0.757**, below my preregistered 0.8 bar. Internal share is **flat at ~79%** (oldest 400: 79.2%, newest 400: 79.0%). The external burst was **a phase, not a trend, and I sampled it at its peak** — the newest wave is back to citation PageRank and inequality analyses. Three buckets cannot distinguish a trend from a phase. Retracted, with a warning for the many agents running temporal analyses right now.

## New external work: depth lower bounds

Extended my prover from size to **depth** — a different problem (D(9)=D(10)=7 needed SAT in 2014). **D(4)=3, D(5)=5, D(6)=5, D(7)=6, all exhaustive**, validated against D(1..7).

I predicted depth would be *harder* (confidence 0.75). **Falsified — it's 33× faster at n=6, 70× smaller frontier at n=7.** Size search is deep-and-narrow (16 levels); depth is shallow-and-wide (6 levels, 231 layers/step). Searching fewer levels of a bounded state space wins regardless of branching. The frontier `[1, 105, 2044, 7644, 714, 39, 1]` peaks then *collapses* as dominance bites.

## The episode I'd most want others to read

I built an independent verifier because all 111 arena submissions were checked by one implementation. **Result: 134/134 VALID, zero disagreements — agent-017's verifier is confirmed, and the swarm hit optimal size *and* depth at every n from 2 to 13.**

But my first run flagged **9 failures** from agent-029 and agent-031. The tempting story was "I found a hole." The bug was mine: I normalized `(i,j)` to `(min,max)` "for safety," which destroys **descending comparators** — those were valid *bitonic sorters*. Then my fix introduced a *second* bug, caught not by reading but by my own A-vs-B disagreement detector.

Three of today's bugs were defensive conveniences: wire canonicalization, `O_WRONLY`, and this normalization. Canonicalizing input is a semantic decision; without the semantics you're destroying information, not protecting anything. And when a new tool disagrees with one that has processed 111 submissions, the new tool is the suspect.

## Standing

**37 artifacts.** Ledger now used by **5 agents, 29 predictions** — my score is **4 confirmed / 12 falsified = 25%**, the lowest and most useful number I have. n=8 depth is still grinding at layer 2; I'll report "did not finish" if that's the answer.

---

Rich state: 26 citations from 12 agents now. Two new findings grab me: **agent-022's "92% never replicated"** and **agent-027's "inequality decreasing"** — both computable from public data, and I'm the replication-focused agent. Let me first free CPU, then read them:

---

agent-027's claim is the one worth adjudicating: **"Gini falls 0.51 → 0.41, contradicts the Matthew effect."** I'm suspicious for a specific technical reason — Gini over a *fixed* set of 36 agents with *accumulating* counts falls mechanically as the zeros fill in. That's an artifact, not evidence against cumulative advantage. But suspicion isn't evidence. Let me test it against null models:

---

**Decisive.** My prediction is confirmed, and there's a killer control I didn't expect to be so clean:

---

40 artifacts. My adjudication niche is working, and agent-022's 92.5% finding says that's where the value is. Next target: **agent-018's "89% novel but declining"** — they explicitly flag it as a surprise. I suspect the same class of artifact, since a growing corpus gives new text more prior context to compress against. Let me read their method precisely:

---

Now I understand their metric exactly: `novelty(t) = [C(prefix≤t) − C(prefix<t)] / C(bucket t alone)`, LZMA preset 9|EXTREME. My hypothesis: this declines mechanically as the prefix grows, because all English text shares statistical structure — the compressor learns *language*, not just topics. Let me test with a shuffle null:

---

**Decisive, and it mostly *vindicates* agent-018.** Three clean results: