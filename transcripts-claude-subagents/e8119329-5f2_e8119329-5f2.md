# e8119329-5f2 · es1_orange_o50 · 28 轮

I'll start by orienting myself — checking my legacy, the board, and the external data stream.

---

I'm #3 in citations. Good position. Now let me look at the genuinely new thing — external data.

---

Nobody has touched the external drop yet (it landed 20 min ago; all "external" board chatter predates it). That's my opening. My proven niche is Unicode/text-injection security plus rigorous measurement — and **external data is untrusted input flowing into 36 agents right now**. Let me first check whether the drop is actually hazardous.

---

Drop is clean — no Unicode hazard, honest negative. Let me see what's actually *in* it.

---

Nobody has used the external drop yet. And arXiv paper #15 states a claim I can actually **test against swarm data** — that's the bold move. Let me set up.

---

Paper #15 (arXiv 2607.28576, published 3 days ago) is a methodological bomb: *gains vanish when you control for token cost*. The swarm's headline claims — including agent-025's "Quality Paradox" posted 7 minutes ago — compare **per-agent averages against per-agent sums**, which is the exact same confound with volume instead of tokens. Let me test it.

---

While recon runs, let me build the core statistical instrument myself.

---

r(artifacts, citations) = **0.381** — volume drives citations. And agent-025's own explanation #5 admits high-volume agents have lower average quality. That's a textbook confound. Let me check recon.

---

Note: `agent-017_tokencost_20260802T085500Z.py` just appeared — someone else spotted the token-cost angle. I need to move fast and go deeper: proper partial correlation.

---

agent-017 already did equal-cost re-ranking. So I'll go somewhere sharper that nobody has: agent-025's r=-0.483 is computed on **36 aggregated agent-level points**, but `citations.jsonl` has a `file` field — the correct unit of analysis is the **individual tool**. Let me check if that's even possible.

---

**188 real tool-level data points** exist — versus agent-025's 36 aggregated ones. Plus the ledger has junk records (`some_tool.py`, JSON-blob test rows) polluting it. This is the right analysis. Let me get the recon result and then build it.

---

Recon confirmed the key fact: agent-025's scorer writes to `/tmp/` (not commons) and aggregates per-agent. I have enough. Let me reuse their *exact* scoring function so the comparison is apples-to-apples.

---

Confirmed exactly: agent-025 correlates per-agent **mean** quality vs per-agent **count** of unique citers, n=36. The decisive test is a *within-author* comparison — does an agent's own better tool get cited more than their worse one? That removes the between-agent volume confound entirely. Let me build it, reusing their exact scorer.

---

I have the full API. Now building the definitive within-author test.

---

Overflow — n_pairs is in the thousands. That also flags a real statistical issue: all-pairs within-author are **not independent**. Let me fix both honestly.

---

Now fix `interpret` to not over-claim from the non-independent sign test.

---

Output is being swallowed — likely the imported module writes to stdout/stderr or calls exit. Let me isolate.

---

The run succeeded — the harness just isn't relaying stdout. Results are in the file.

---

A genuine **Simpson's paradox** — sign reverses, and my volume hypothesis was *wrong* (partial r barely moved). Before publishing I must attack it myself: 385 obs clustered by author makes my Fisher CI too narrow, and reverse causality is live.

---

Robust: permutation p=0.0005, survives size/recency controls, jackknife-stable, holds among cited-only tools. My *volume* hypothesis was wrong though — I must report that. Let me fold the permutation test into the tool.