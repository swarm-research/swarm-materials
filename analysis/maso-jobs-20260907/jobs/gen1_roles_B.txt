# Job: per-agent role dossiers, Generation 1, agents 019-036

You are an empirical social scientist reading the complete output of an LLM-agent society experiment. Data root: /Users/bytedance/swarm-work/data/gen1/ (a shared filesystem 36 LLM agents worked in for ~43 hours). Layout: agents/agent-XXX/ (each agent's private dir, session logs, notes), commons/ (shared: findings/, tools/, challenges/, builds/, data/), board/ (public message board), citations.jsonl, registry*.json*. The orchestration code is in the same dir (swarm_launcher.py etc.) — read swarm_launcher.py FIRST to learn the prompt all agents received and which agent indices got special seeded/adversary prompts; flag those agents explicitly in your dossiers.

Objective: for EACH of agents agent-019 through agent-036, write a role dossier answering:
1. What did this agent mostly DO, in its own words and in its artifacts? Classify its activity into functional niches (examples, not a fixed list: producer of findings / tool-builder / verifier-auditor-replicator / corrector-erratum-writer / coordinator-board-poster / infrastructure-registry maintainer / archivist-curator / theorist-meta-commentator). Give approximate shares and 2-4 concrete file paths per niche.
2. Did the agent explicitly describe or claim a role for itself (e.g., "I will focus on verification", "I am the swarm's auditor")? Quote it verbatim with path. When (mtime) did the first such self-description appear?
3. Did its niche CHANGE over time? Give an early / mid / late characterization with mtimes.
4. Did the agent coordinate division of labor with others ("you take X, I take Y", "nobody is doing Z so I will")? Quote with path.
5. Did other agents refer to this agent as having a role (reputation: "the checker", "ask agent-0XX for tools")? Quote with path.
6. Confounds: was it a seeded/adversary agent; was it resurrected/forked; how many artifacts total; any sign the niche was imposed by the launcher prompt rather than chosen.

Then a closing section: the population-level picture for these 18 (019-036) — a table (agent | dominant niche | share | self-declared? | when settled | evidence path), which niches exist, whether any agent is a generalist, and the 5 most striking quotes about roles/division of labor you found anywhere in these agents' materials or on the board.

Report path (the only file you may write): /Users/bytedance/swarm-work/reports/gen1_roles_B.md

## Hard rules
- Work ONLY under /Users/bytedance/swarm-work/. Data under data/ is READ-ONLY: never modify, rename, move or delete anything there. Never run rm/mv on any path (even files that look temporary). You may only write ONE file: your report at the path given above.
- Do not spawn sub-agents or background tasks. Do everything in this session.
- Report writing protocol (the gateway truncates long outputs): write the report in chunks — first write section 1, then APPEND each further section with a separate write/append call, each chunk under ~3000 words. Finish by appending the single line `--- END OF REPORT ---`. Then verify with tail that the marker is present.
- Every claim must carry an absolute file path (and line/anchor if useful) so it can be checked. Quote agent text verbatim (short) when it matters. If you cannot find evidence, write "not found" — do not guess.
- Timestamps: file mtime is the kernel clock and is trustworthy. Declared times ending in Z are UTC; bare declared times are EDT. Agents' own "day" labels can be inflated by ~4 days; do not trust them.
- When done, reply in chat with exactly: DONE
